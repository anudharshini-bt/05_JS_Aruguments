var message;
var showMessage;

message = "Hello, world!";

showMessage = function () {
	console.log(message);
};

showMessage();

var message;
var showMessage;

message = "It's full of stars!";

showMessage = function () {
	console.log(message); // Changed variable name to 'msg'
};

showMessage();